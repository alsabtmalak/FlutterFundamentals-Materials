/* Example 4.2.2 - Creating Lists: Using the List Constructor */

void main() {

  // Creating a fixed-length list of doubles, initially filled with 0.0
List<double> decimals = List.filled(3, 0.0); //[0.0,0.0,0.0]
print(decimals);
}